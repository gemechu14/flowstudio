# FlowStudio — copy to .env.local for local development

# LLM providers (used when keys are not set in Settings UI)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Optional database for data sources
DATABASE_URL=
